{
  "name": "temperaturcrowd-server",
  "version": "1.0.0",
  "description": "TemperaturCrowd Monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r --if-present run build",
    "start": "pnpm -r start",
    "lint": "pnpm -r --if-present run lint",
    "test": "pnpm -r --if-present run test"
  },
  "dependencies": {
    "@seven.io/client": "^5.6.0",
    "api": "^6.1.3"
  },
  "devDependencies": {
    "es5-ext": "^0.10.64"
  },
  "pnpm": {
    "overrides": {
      "brace-expansion": "^5.0.8",
      "fast-uri": "^3.1.4",
      "find-my-way": "^9.7.0",
      "@fastify/static": "^10.1.2",
      "underscore": "^1.13.8",
      "postcss": "^8.5.18"
    }
  }
}
